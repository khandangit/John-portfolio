---
layout: layouts/base.njk
title: Home
permalink: "/"
---
<section class="wrap hero hero-flex hero-rounded">
  <!-- Background glow -->
  <div class="hero-bg" aria-hidden="true"></div>

  <div class="hero-text">
    <h1>Welcome to John’s Portfolio</h1>
    <p>Hi everyone,</p>
    <p>Thanks for visiting my website.</p>
    <p>I am John, and here I share my latest artworks and portraits.</p>
    <p><a href="/gallery/" class="btn">View Gallery →</a></p>
  </div>

  <div class="hero-image">
    <img src="/assets/img/the_artist.jpg" alt="John the Artist">
  </div>
</section>

<style>
/* ===== Hero layout ===== */
.hero-flex {
  display: flex;
  gap: 2rem;
  align-items: flex-start;            /* top-align columns */
  justify-content: space-between;
  position: relative;
  overflow: hidden;
  padding: 1.25rem 2rem;              /* compact top spacing */
}

/* Rounded card */
.hero-rounded {
  border-radius: 20px;
  background: var(--card, #111);
  box-shadow: 0 8px 30px rgba(0,0,0,0.18);
}

/* Keep content above glow */
.hero-text,
.hero-image { position: relative; z-index: 1; }
.hero-text { flex: 1; }
.hero-text h1 { margin: 0.1rem 0 0.6rem; line-height: 1.15; }
.hero-text p { margin: 0.3rem 0; }

/* Image sizing (responsive) */
.hero-image { align-self: flex-start; }
.hero-image img {
  width: 360px;        /* desktop base */
  height: auto;
  border-radius: 14px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.15);
}

/* Green glow background */
.hero-bg {
  position: absolute;
  inset: -30% -30% -30% -30%;
  background: radial-gradient(
      circle at 40% 40%,
      rgba(110, 231, 183, 0.20) 0%,
      rgba(110, 231, 183, 0.12) 35%,
      transparent 80%
    );
  filter: blur(90px);
  z-index: 0;
  border-radius: inherit;
  animation: glowPulse 14s ease-in-out infinite alternate;
}

@keyframes glowPulse {
  0%   { opacity: 0.55; transform: scale(1); }
  100% { opacity: 0.9;  transform: scale(1.12); }
}

/* ===== Responsive tweaks ===== */
@media (max-width: 1024px) {
  .hero-image img { width: 320px; }
}

@media (max-width: 768px) {
  .hero-flex { flex-direction: column; text-align: center; align-items: center; }
  .hero-image { align-self: center; }
  .hero-image img { width: 260px; }
}

@media (max-width: 480px) {
  .hero-image img { width: 220px; }
}
</style>
